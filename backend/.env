PORT=9050
NODE_ENV=development

# Redis (Local or VPS)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# ─── SSH Tunnel (to reach VPS MySQL) ───────────────────────────────────────
SSH_HOST=206.183.129.220
SSH_PORT=22
SSH_USER=mohamoud
SSH_PASS=Abc123@@

# ─── Database (MySQL on VPS via SSH tunnel) ─────────────────────────────────
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=duc_user
DB_PASS=Ducaysane@2026!System
DB_NAME=hotel_app

# Local port that the tunnel will bind to (changed because 3307 is taken locally)
DB_LOCAL_PORT=3310

# Set this to true if you are still testing locally via SSH tunnel
USE_SSH_TUNNEL=true

# ─── JWT ────────────────────────────────────────────────────────────────────
JWT_SECRET=somstay_super_secret_key_2026
JWT_EXPIRES_IN=7d

# Elasticsearch
ELASTICSEARCH_URL=http://206.183.129.220:9200
IMAGE_BASE_URL=http://localhost:9050/uploads/
